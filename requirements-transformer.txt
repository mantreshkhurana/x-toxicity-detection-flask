# Optional: the transformer backend (TOXICITY_BACKEND=transformer).
# Buys a few points of macro F1 for ~1-3 GB of RAM and a multi-gigabyte
# checkpoint download, so it is deliberately kept out of requirements.txt.
#
#   pip install -r requirements.txt -r requirements-transformer.txt
#   TOXICITY_BACKEND=transformer python app.py
-r requirements.txt
torch==2.5.1
transformers==4.46.3
sentencepiece==0.2.0
